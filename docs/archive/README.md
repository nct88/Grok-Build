# Archive — VS Code era

Documents and strategies from when Grok Build 1.0 tried to **skin Code-OSS** as agent-first (dual product surfaces inside one VS Code binary).

That approach is **superseded** by:

- Primary: `apps/desktop` + `packages/acp-client`
- Optional: `ide` Code-OSS only as **Grok Build IDE**
