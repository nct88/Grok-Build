# Grok Build Desktop 0.5.34

<!-- release:vi -->
<!-- release:en -->

| Tiếng Việt | English |
|---|---|
| Bản phát hành 0.5.34 được chuẩn bị ngày 13-08-2026 sau khi chuyển bộ chọn thư mục dự án xuống sát khung nhập chat. Các tệp Windows chưa được ký số. | Release 0.5.34 was prepared on 2026-08-13 after moving the project folder picker onto the chat composer. The Windows artifacts are unsigned. |

## Nội dung phát hành / Release highlights

| Tiếng Việt | English |
|---|---|
| Bộ chọn thư mục dự án nằm ngay phía trên, góc trái khung nhập tin nhắn — không còn ở thanh tiêu đề hội thoại. | The project folder picker sits on the top-left of the message box instead of the conversation header. |

## Gói phát hành / Release artifacts

- `Grok-Build-Setup-0.5.34.exe` — NSIS Setup / Bộ cài NSIS
- `Grok-Build-0.5.34-win32-x64-portable.exe` — Portable executable / Bản portable EXE
- `Grok-Build-0.5.34-win32-x64.zip` — Portable ZIP / Bản ZIP giải nén
- `MANIFEST.json` — Sizes and SHA-256 / Kích thước và SHA-256

| Tiếng Việt | English |
|---|---|
| Các tệp Windows chưa được ký Authenticode và có thể kích hoạt cảnh báo SmartScreen. Hãy kiểm tra SHA-256 trong `MANIFEST.json` trước khi chạy. | The Windows artifacts are not Authenticode-signed and may trigger SmartScreen. Verify SHA-256 values in `MANIFEST.json` before running them. |

Trang phát hành / Release page: https://github.com/nct88/Grok-Build-Desktop/releases/tag/v0.5.34

## Kiểm chứng / Verification

| Tiếng Việt | English |
|---|---|
| Kiểm thử Electron đồng bộ picker/sidebar/tab và di chuyển session giữa hai dự án tạm. | The Electron integration verifies picker/sidebar/tab synchronization and a real session move between two temporary projects. |
| `npm run check` chạy kiến trúc, đóng gói, thương hiệu, hợp đồng release, E2E và visual gate. | `npm run check` covers architecture, packaging, brand, release contract, E2E and visual gates. |
| Ảnh kiểm chứng layout 1000×640 xác nhận picker nằm trên góc trái khung nhập. | Visual evidence at 1000×640 confirms the picker on the top-left of the composer. |
